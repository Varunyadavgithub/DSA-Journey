package StringBuilder;
import java.util.*;
public class decleration {
    public static void main(String[] args) {
        // StringBuilder s=new StringBuilder();//Creating an empty StringBuilder, initial capacity is 16
        // StringBuilder str=new StringBuilder("Hello");//Creating a StringBuilder with specified String
        // StringBuilder gtr=new StringBuilder(100);//Creating empty StringBuilder with specified capacity/length
        Scanner sc=new Scanner(System.in);
        StringBuilder n=new StringBuilder(sc.nextLine());
    }
}
